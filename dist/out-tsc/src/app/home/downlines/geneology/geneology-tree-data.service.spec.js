import { TestBed } from '@angular/core/testing';
import { GeneologyTreeDataService } from './geneology-tree-data.service';
describe('GeneologyTreeDataService', function () {
    beforeEach(function () { return TestBed.configureTestingModule({}); });
    it('should be created', function () {
        var service = TestBed.get(GeneologyTreeDataService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=geneology-tree-data.service.spec.js.map